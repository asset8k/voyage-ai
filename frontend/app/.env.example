# FastAPI backend base URL used by the Vite development server.
VITE_API_BASE_URL=http://localhost:8000

# Browser-restricted key for the Google Maps JavaScript API. This value is exposed
# to the browser, so restrict it to local and deployed frontend URLs.
VITE_GOOGLE_MAPS_API_KEY=
