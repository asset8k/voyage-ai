[project]
name = "voyage-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Asset", email = "asset8k@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
voyage-ai = "voyage_ai:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
